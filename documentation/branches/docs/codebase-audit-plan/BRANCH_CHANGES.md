# Branch Changes — docs/codebase-audit-plan

| File Name                                                                                                              | Summary of Changes                                                                                                                                                   | Summary Reason for Change                                                                                               | Expected Impact                                                                                                               | Best Practice Evaluation Score                                                       | Reason for Deletion                                                                                   |
| ---------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------- |
| `todo.md`                                                                                                              | Consolidates five area audits, orders remediation, and records completion status.                                                                                    | Preserve the evidence, priorities, and implementation handoff in one canonical artifact.                                | Reviewers can trace every implementation package to an audited finding.                                                       | 9/10 — evidence-led and dependency ordered.                                          | not deleted                                                                                           |
| `package.json`, `pnpm-workspace.yaml`, `pnpm-lock.yaml`                                                                | Pins pnpm 10.28.2, adds worker scripts and coverage tooling, and centralizes pnpm overrides.                                                                         | Make local and CI dependency behavior deterministic.                                                                    | Reproducible installs and directly runnable production workers.                                                               | 9/10 — pinned toolchain with lockfile parity.                                        | not deleted                                                                                           |
| `.github/workflows/ci.yml`, `.github/workflows/security.yml`, `scripts/hooks/**`                                       | Adds Redis, coverage, E2E, policy validation, pinned pnpm, and blocking hook behavior.                                                                               | Replace incomplete/noisy quality gates with enforceable checks.                                                         | CI now detects browser, coverage, hook-policy, lint, type, and test regressions.                                              | 9/10 — coverage is ratcheted from the measured baseline.                             | not deleted                                                                                           |
| `src/db/schema.ts`, `drizzle/0013_*.sql`–`drizzle/0018_*.sql`, `drizzle/meta/**`                                       | Adds attempt idempotency/lease constraints, plan version fences, verification fields, webhook leases, and correlation IDs.                                           | Persist the invariants required by reliable orchestration.                                                              | Prevents duplicate active attempts and stale/duplicate plan materialization while enabling durable delivery.                  | 9/10 — additive generated migrations; no hand-edited SQL.                            | not deleted                                                                                           |
| `src/lib/credential-crypto.ts`, `src/lib/agent-config-public.ts`, `src/lib/agent-auth.ts`, integration helpers         | Encrypts stored credentials, redacts browser DTOs, validates token lifecycle/scope, and constrains outbound repository checks.                                       | Close credential disclosure, expired-token, tenant-scope, and arbitrary outbound-request defects.                       | Secrets remain server-side and agent credentials operate only in their authorized project.                                    | 9/10 — authenticated encryption and centralized boundary validation.                 | not deleted                                                                                           |
| `src/repositories/agent-config-repository.ts`, `token-repository.ts`, `webhook-repository.ts`, `project-repository.ts` | Moves credential handling and project-scoped token rules into repositories.                                                                                          | Ensure route checks cannot be bypassed by alternate callers.                                                            | Consistent storage and authorization invariants.                                                                              | 9/10 — repository boundary is the source of truth.                                   | not deleted                                                                                           |
| `src/repositories/task-repository.ts`, `agent-session-repository.ts`, task/session agent routes                        | Implements atomic task claims, owned attempt completion, idempotency keys, exact session accounting, cancellation, and ghost recovery.                               | Repair concurrency, cross-session attribution, and stuck-work behavior.                                                 | At-most-one active lease per task and recoverable bounded execution.                                                          | 9/10 — conditional updates and row locking enforce state transitions.                | not deleted                                                                                           |
| `scripts/agent.ts`, `scripts/ghost-buster.ts`, `scripts/recovery-worker.ts`                                            | Adds independent jittered heartbeat/cancellation polling, bounded child processes, verification execution, and continuous recovery.                                  | Keep long tasks healthy and recover abandoned leases automatically.                                                     | Fewer false ghost resets and no indefinitely occupied execution slots.                                                        | 9/10 — bounded processes with graceful/forced termination.                           | not deleted                                                                                           |
| `scripts/plan-worker.ts`, plan/spec repositories and routes                                                            | Adds immutable version/generation fences and atomic DAG materialization.                                                                                             | Prevent stale plan jobs and partial or duplicate task graphs.                                                           | Editing during generation cannot resurrect superseded work.                                                                   | 9/10 — validates before a fenced transaction.                                        | not deleted                                                                                           |
| `src/lib/webhooks.ts`, `scripts/webhook-worker.ts`, webhook routes/components                                          | Replaces in-process retry timers with leased persisted deliveries, bounded requests/body reads, jitter, exhaustion state, encrypted secrets, and redacted responses. | Make outbound integration delivery restart-safe and bounded.                                                            | Webhook retries survive process restarts and cannot monopolize app requests.                                                  | 8/10 — durable queue is complete; external queue dashboards remain deployment-owned. | not deleted                                                                                           |
| `src/proxy.ts`, auth/agent routes, `src/lib/rate-limiter.ts`, `src/lib/redis.ts`                                       | Adds request IDs and tiered API/auth/agent rate limiting with bounded Redis behavior.                                                                                | Provide abuse resistance and traceable request context.                                                                 | Requests receive correlation headers and excessive callers receive 429 responses.                                             | 8/10 — in-process browser limit is suitable for the current Compose topology.        | not deleted                                                                                           |
| `infra/docker/**`, `infra/compose/docker-compose.yml`, health routes, `documentation/infrastructure/OPERATIONS.md`     | Separates migration and runtime processes, removes production seeding, starts all workers, and defines probes.                                                       | Make the supplied deployment topology operate the complete product safely.                                              | Clean startup, supervised background work, and correct traffic readiness.                                                     | 9/10 — explicit one-shot migration and separate workloads.                           | not deleted                                                                                           |
| spec, project, session, task, and settings UI files                                                                    | Repairs save/generate/regenerate, project selection, session CTA, manual task intervention, approval confirmation, secret forms, and narrow-screen layouts.          | Restore incomplete product flows and remove unsafe client behavior.                                                     | Core workflows are usable, permission-correct, and responsive.                                                                | 9/10 — uses existing components and design tokens.                                   | not deleted                                                                                           |
| `src/app/layout.tsx`, `src/app/globals.css`, shared headers/chrome, `DESIGN_SYSTEM.md`                                 | Replaces Inter/JetBrains Mono with Source Sans 3/Fira Code, raises shared compact-control sizes, and removes duplicate current-page headings.                        | Improve readability and give the refreshed identity a deliberate developer-tool voice.                                  | Interface prose scans more easily, technical data remains distinct, and each screen has one page heading.                     | 9/10 — centralized font loading and shared sizing prevent page-by-page drift.        | not deleted                                                                                           |
| `scripts/capture-readme-screenshots.ts`, `public/screenshots/*.png`, `README.md`                                       | Adds deterministic Playwright capture automation and refreshes the complete README gallery with stable filenames.                                                    | Keep product imagery synchronized with the application without repeating a manual capture process.                      | README visuals now show the current identity, typography, and heading hierarchy.                                              | 9/10 — seeded fixtures, explicit readiness checks, and fixed capture settings.       | Superseded date-stamped screenshots were removed.                                                     |
| `src/lib/webhooks.ts`, `src/repositories/plan-job-repository.ts`, `scripts/plan-worker.ts`                             | Contains fire-and-forget webhook enqueue failures and adds generation-token-bound worker heartbeats.                                                                 | Address independent-review findings that could cause unhandled rejections or falsely fail legitimate long-running jobs. | Background failures are logged without destabilizing request processes, while active jobs retain their leases.                | 9/10 — failure isolation and conditional heartbeats are regression tested.           | not deleted                                                                                           |
| `src/app/api/v1/tasks/[id]/route.ts`, `src/repositories/task-repository.ts`                                            | Persists validated title edits, gives task validation errors their intended response shape, and preserves explicit zero-cost updates.                                | Resolve adjacent task-update defects found while validating the independent review.                                     | Task edits no longer report success without persisting, clients receive useful 400 errors, and cost accounting remains exact. | 9/10 — boundary and repository behavior are both integration tested.                 | not deleted                                                                                           |
| `tests/**`, `vitest.config.ts`, `src/types/styles.d.ts`, `documentation/infrastructure/TESTING_HANDBOOK.md`            | Updates lease-aware fixtures, removes placeholder coverage, adds browser and review-regression assertions, and enforces measured coverage floors.                    | Keep tests aligned with the strengthened data model and make coverage honest.                                           | 98 tests and the protected-entry browser journey pass; coverage cannot regress.                                               | 9/10 — deterministic database isolation and ratcheted coverage.                      | `tests/home.test.tsx` and its snapshot were deleted because they only asserted a placeholder literal. |

## CI and test changes

- CI now provisions PostgreSQL and Redis, migrates the database, runs coverage and Playwright, and uploads failure reports.
- A repository-policy job validates hook integrity and shell syntax.
- Test databases are isolated and serialized; retry handling recognizes nested PostgreSQL lock/deadlock codes.
- Coverage is enforced at the measured baseline and documented as an upward-only ratchet toward 80%.
- The E2E smoke test now verifies that an unauthenticated protected route reaches a usable login form.
- CI seeds the canonical demo fixtures before Playwright, whose authenticated shell check enforces one authoritative `Mission Control` heading.
- Independent-review regressions cover webhook failure containment, live plan-job leases, title persistence, task validation responses, and exact zero-cost accounting.
