name: Release

on:
  workflow_run:
    workflows: ['CI']
    types: [completed]
    branches: [main]

permissions:
  contents: read

jobs:
  deploy:
    name: Deploy
    # Only run when the triggering CI workflow succeeded
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      # TODO: Replace this placeholder with the actual deployment command once
      # deployment infrastructure is configured (e.g., Vercel, Fly.io, Railway).
      - name: Deploy (placeholder)
        run: echo "Deployment would run here. Configure deploy target and replace this step."
