name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  NODE_OPTIONS: "--dns-result-order=ipv4first"

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: specdrivr_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
      redis:
        image: redis:7
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379

    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Check for forbidden artifacts
        run: |
          if [ -f "fix-tsc.sh" ] || [ -f "fix-lint2.sh" ] || ls fix-*.mjs 1> /dev/null 2>&1; then
            echo "Error: Temporary fix scripts found."
            exit 1
          fi

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Debug Environment Variables
        run: |
          echo "DATABASE_URL: ${DATABASE_URL:-not set}"
          echo "REDIS_URL: ${REDIS_URL:-not set}"
          echo "NEXTAUTH_URL: ${NEXTAUTH_URL:-not set}"
          echo "NODE_OPTIONS: ${NODE_OPTIONS:-not set}"
        env:
          DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/specdrivr_test
          REDIS_URL: redis://127.0.0.1:6379
          NEXTAUTH_URL: http://127.0.0.1:3000

      - name: Run database migrations
        run: pnpm db:migrate
        env:
          DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/specdrivr_test

      - name: Run unit tests
        run: pnpm vitest run
        env:
          CI: true
          DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/specdrivr_test
          REDIS_URL: redis://127.0.0.1:6379
          NEXTAUTH_URL: http://127.0.0.1:3000
          NEXTAUTH_SECRET: test-secret-for-ci-must-be-32-chars-long
          BETTER_AUTH_SECRET: test-secret-for-ci-must-be-32-chars-long
          RESEND_API_KEY: re_dummy_key_for_ci
          RESEND_FROM_EMAIL: noreply@specdrivr.dev

      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps

      - name: Run E2E tests
        run: pnpm test:e2e
        env:
          CI: true
          DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/specdrivr_test
          REDIS_URL: redis://127.0.0.1:6379
          NEXTAUTH_URL: http://127.0.0.1:3000
          NEXTAUTH_SECRET: test-secret-for-ci-must-be-32-chars-long
          BETTER_AUTH_SECRET: test-secret-for-ci-must-be-32-chars-long
          RESEND_API_KEY: re_dummy_key_for_ci
          RESEND_FROM_EMAIL: noreply@specdrivr.dev
